<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cc.feitwnd.mapper.FriendLinkMapper">
    <insert id="insert">
        insert into friend_links (name, url, avatar_url, description, sort, is_visible, create_time, update_time)
        values (#{name}, #{url}, #{avatarUrl}, #{description}, #{sort}, #{isVisible}, #{createTime}, #{updateTime})
    </insert>
    <update id="update">
        update friend_links
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="url != null">url = #{url},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="description != null">description = #{description},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="isVisible != null">is_visible = #{isVisible},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        where id = #{id}
    </update>
    
    <delete id="batchDelete">
        delete from friend_links where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
