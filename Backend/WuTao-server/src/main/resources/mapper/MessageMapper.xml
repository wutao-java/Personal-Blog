<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cc.wutao.mapper.MessageMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into messages
            (content, content_html, root_id, parent_id, parent_nickname, visitor_id, 
             nickname, email_or_qq, location, user_agent_os, user_agent_browser,
             is_approved, is_markdown, is_secret, is_notice, is_edited, is_admin_reply,
             create_time, update_time)
        values
            (#{content}, #{contentHtml}, #{rootId}, #{parentId}, #{parentNickname}, #{visitorId},
             #{nickname}, #{emailOrQq}, #{location}, #{userAgentOs}, #{userAgentBrowser},
             #{isApproved}, #{isMarkdown}, #{isSecret}, #{isNotice}, #{isEdited}, #{isAdminReply},
             #{createTime}, #{updateTime})
    </insert>

    <select id="pageQuery" resultType="cc.wutao.entity.Messages">
        select * from messages
        <where>
            <if test="isApproved != null">
                and is_approved = #{isApproved}
            </if>
            <if test="startTime != null">
                and create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and create_time &lt;= #{endTime}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="getById" resultType="cc.wutao.entity.Messages">
        select * from messages where id = #{id}
    </select>

    <update id="batchApprove">
        update messages
        set is_approved = 1, update_time = now()
        where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="batchDelete">
        delete from messages
        where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        or root_id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- ===== 博客端查询 ===== -->

    <select id="getApprovedList" resultType="cc.wutao.vo.MessageVO">
        select id, root_id, parent_id, parent_nickname, content, content_html, is_markdown,
               visitor_id, nickname, email_or_qq, location, user_agent_os, user_agent_browser,
               is_approved, is_secret, is_admin_reply, create_time
        from messages
        where (is_approved = 1 or visitor_id = #{visitorId})
        order by create_time asc
    </select>

    <update id="updateContent">
        update messages
        set content = #{content}, content_html = #{contentHtml},
            is_edited = 1, update_time = now()
        where id = #{id}
    </update>

</mapper>
