server:
  port: 5922

spring:
  application:
    name: WuTaoBackend
  profiles:
    active: dev #环境配置dev/prod
  threads:
    virtual:
      enabled: true #启用虚拟线程
  main:
    allow-circular-references: true #允许循环引用
  servlet:
    multipart:
      max-file-size: 30MB      # 单个文件最大大小
      max-request-size: 50MB   # 请求体最大大小
  #数据库连接信息
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: ${wutao.datasource.driver-class-name}
    url: jdbc:mysql://${wutao.datasource.host}:${wutao.datasource.port}/${wutao.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: ${wutao.datasource.username}
    password: ${wutao.datasource.password}
    # Druid连接池专用配置
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
  data:
    redis:
      host: ${wutao.redis.host}
      port: ${wutao.redis.port}
      password: ${wutao.redis.password}
      database: ${wutao.redis.database}

  mail:
    host: smtp.qq.com
    port: 587
    username: # qq邮箱
    password: # 邮箱授权码
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

mybatis:
  #mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cc.wutao.entity
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true

#配置事务管理日志级别
logging:
  level:
    cc:
      wutao:
        mapper: debug
        service: info
        controller: info

wutao:
  jwt:
    # 设置jwt签名加密时使用的秘钥
    secret-key: your-jwt-secret-key-here
    # 设置jwt过期时间
    ttl: ${wutao.jwt.ttl}
    # 设置前端传递过来的令牌名称
    token-name: Authorization
  alioss:
    endpoint: your-oss-endpoint-here
    access-key-id: your-alioss-access-key-id
    access-key-secret: your-alioss-access-key-secret
    bucket-name: your-bucket-name
  email:
    personal:    #SMTP邮件发送者昵称
    from:   #SMTP邮件发送者邮箱，跟上面那个邮箱配置的username一致即可
    admin-notify-to:  #新评论和留言通知收件邮箱
  visitor:
    verify-code:   #管理端游客登录用的邮箱验证码
  image:
    compress:
      enabled: true           # 是否启用图片压缩
      max-size-kb: 500          # 最大500KB
      quality: 0.9              # 压缩质量(0-1)
      output-format: webp       # 输出格式: webp
      max-dimension: 2560       # 图片最长边像素上限，超过则等比缩小，防止大图解码占用巨量内存
  website:
    title:       # 网站标题(或你的昵称)
    home:        # 首页地址
    admin:       # 后台管理地址
    cv:          # 个人简历地址
    blog:        # 博客地址
  # AI 摘要模块配置（可插拔扩展模块）
  # 重要：打包是否包含 AI 模块由构建期的 Maven profile 决定（-Pwith-ai），本配置的 enabled 只控制运行时是否启用。
  # 即：打包时不带 -Pwith-ai，无论 enabled 为何值，AI 模块和 langchain4j 都不会进入包体；
  #     打包时带 -Pwith-ai，包体包含 AI 模块，此时 enabled=true 才真正启用 AI 功能。
  # 秘钥务必通过环境变量注入，不要直接写死在 yml 中
  # 各功能（摘要/错别字修正等）的提示词存放在 ai 模块内部 prompt 包下的常量类中，不在 yml 中配置
  ai:
    enabled: false                          # 是否启用AI摘要生成
    base-url: https://api.deepseek.com/v1   # 模型接口地址(OpenAI兼容协议)
    api-key:                                # API秘钥
    model-name: deepseek-v4-flash           # 模型名称
    temperature: 0.3                        # 采样温度
    timeout-seconds: 180                     # 请求超时时间(秒)
