# WuTao Website 宝塔部署指南

> 适用于 2核2G 内存服务器，Spring Boot 3 + Vue 3 + MySQL 8 + Redis 7 全栈项目

---

## 目录

1. [环境准备](#1-环境准备)
2. [创建数据库](#2-创建数据库)
3. [配置后端](#3-配置后端)
4. [上传部署文件](#4-上传部署文件)
5. [创建站点与 Nginx 配置](#5-创建站点与-nginx-配置)
6. [启动后端服务](#6-启动后端服务)
7. [DNS 解析配置](#7-dns-解析配置)
8. [2G 内存服务器优化](#8-2g-内存服务器优化)
9. [SSL 证书配置](#9-ssl-证书配置可选)
10. [常见问题排查](#10-常见问题排查)

---

## 1. 环境准备

### 1.1 宝塔安装软件

在宝塔 **软件商店** 中安装以下软件：

| 软件 | 版本 | 用途 |
|---|---|---|
| Nginx | 任意 | 反向代理 + 静态文件托管 |
| MySQL | 8.0 | 数据库 |
| Redis | 7.x | 缓存 |
| JDK | 21 | 运行 Spring Boot |

> **注意**：不需要安装 Tomcat。Spring Boot 内置 Tomcat，独立安装 Tomcat 会白白占用内存。

### 1.2 安全组放行端口

在阿里云安全组中放行以下端口：

| 端口 | 用途 |
|---|---|
| 80 | HTTP |
| 443 | HTTPS |
| 5922 | 后端服务（可不对公网开放，仅本机访问） |

---

## 2. 创建数据库

### 2.1 添加数据库

宝塔 **数据库** → **添加数据库**：

| 字段 | 值 |
|---|---|
| 数据库名 | `personal` |
| 用户名 | `personal` |
| 密码 | 自定义强密码 |
| 字符集 | `utf8mb4` |

### 2.2 导入建表脚本

进入数据库管理 → 导入 `Backend/WuTao-server/src/main/resources/database/wutao.sql`

---

## 3. 配置后端

### 3.1 application.yml 主配置

确保 `spring.profiles.active` 为 `prod`：

```yaml
spring:
  profiles:
    active: prod
```

同时配置网站域名（等 DNS 解析配好后改为 `https://`）：

```yaml
wutao:
  website:
    home: http://www.wutao.tech
    admin: http://admin.wutao.tech
    cv: http://cv.wutao.tech
    blog: http://wutao.tech
```

### 3.2 application-prod.yml 生产配置

从 `application-prod.yml.template` 复制为 `application-prod.yml`，填入实际配置：

```yaml
wutao:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    host: 127.0.0.1          # 本机访问，不要用公网IP
    port: 3306
    database: personal
    username: personal
    password: 你的数据库密码
  redis:
    host: 127.0.0.1          # 本机访问，不要用公网IP
    port: 6379
    password: 你的Redis密码
    database: 0
  jwt:
    ttl: 604800000           # 7天有效期
```

### 3.3 配置要点

| 配置项 | 注意事项 |
|---|---|
| MySQL/Redis host | 必须用 `127.0.0.1`，不要用公网 IP |
| 数据库名 | `personal`（标准命名） |
| 数据库用户名 | `personal`（标准命名） |
| 密码 | 使用强密码，不要用 `123456` |
| 邮箱 | 后续配置，先不影响部署 |
| 域名 | 暂用 `http://`，SSL 配好后改 `https://` |

---

## 4. 上传部署文件

### 4.1 后端文件

上传到服务器 `/www/wwwroot/wutao/` 目录：

| 文件 | 本地路径 | 服务器路径 |
|---|---|---|
| JAR 包 | `Backend\WuTao-server\target\WuTao-server-1.0-SNAPSHOT.jar` | `/www/wwwroot/wutao/WuTao-server-1.0-SNAPSHOT.jar` |
| 主配置 | `Backend\WuTao-server\src\main\resources\application.yml` | `/www/wwwroot/wutao/application.yml` |
| 生产配置 | `Backend\WuTao-server\src\main\resources\application-prod.yml` | `/www/wwwroot/wutao/application-prod.yml` |

### 4.2 前端文件

4 个前端 dist 目录内容分别上传到对应站点目录：

| 前端 | 本地 dist 路径 | 服务器路径 |
|---|---|---|
| 博客 | `Frontend-Blog\dist\` 内容 | `/www/wwwroot/wutao.tech/` |
| 管理端 | `Frontend-Admin\dist\` 内容 | `/www/wwwroot/admin.wutao.tech/` |
| 主页 | `Frontend-Home\dist\` 内容 | `/www/wwwroot/www.wutao.tech/` |
| 简历 | `Frontend-Cv\dist\` 内容 | `/www/wwwroot/cv.wutao.tech/` |

> **注意**：上传的是 dist **里面的内容**（index.html、assets 等），不是 dist 文件夹本身。上传后每个目录下应有 `index.html`。

### 4.3 本地构建命令

如需重新构建前端：

```bash
# 博客端
cd Frontend-Blog && pnpm install && pnpm build

# 管理端
cd Frontend-Admin && pnpm install && pnpm build

# 主页
cd Frontend-Home && pnpm install && pnpm build

# 简历
cd Frontend-Cv && pnpm install && pnpm build
```

构建后端 JAR：

```bash
cd Backend
mvn clean package -DskipTests
# 产出：WuTao-server/target/WuTao-server-1.0-SNAPSHOT.jar
```

---

## 5. 创建站点与 Nginx 配置

### 5.1 创建 4 个站点

宝塔 **网站** → **添加站点**，逐个添加：

| 域名 | 根目录 | PHP 版本 | FTP | 数据库 |
|---|---|---|---|---|
| `wutao.tech` | `/www/wwwroot/wutao.tech` | 纯静态 | 不创建 | 不创建 |
| `www.wutao.tech` | `/www/wwwroot/www.wutao.tech` | 纯静态 | 不创建 | 不创建 |
| `admin.wutao.tech` | `/www/wwwroot/admin.wutao.tech` | 纯静态 | 不创建 | 不创建 |
| `cv.wutao.tech` | `/www/wwwroot/cv.wutao.tech` | 纯静态 | 不创建 | 不创建 |

### 5.2 Nginx 配置 — 博客站（wutao.tech）

站点 **设置** → **配置文件**，替换为以下内容：

```nginx
server
{
    listen 80;
    server_name wutao.tech;
    index index.html;
    root /www/wwwroot/wutao.tech;
    #CERT-APPLY-CHECK--START
    # 用于SSL证书申请时的文件验证相关配置 -- 请勿删除
    include /www/server/panel/vhost/nginx/well-known/wutao.tech.conf;
    #CERT-APPLY-CHECK--END
    include /www/server/panel/vhost/nginx/extension/wutao.tech/*.conf;
    
    #SSL-START SSL相关配置，请勿删除或修改下一行带注释的404规则
    #error_page 404/404.html;
    #SSL-END

    #ERROR-PAGE-START  错误页配置，可以注释、删除或修改
    error_page 404 /404.html;
    #error_page 502 /502.html;
    #ERROR-PAGE-END

    #PHP-INFO-START  PHP引用配置，可以注释或修改
    include enable-php-00.conf;
    #PHP-INFO-END

    #REWRITE-START URL重写规则引用,修改后将导致面板设置的伪静态规则失效
    include /www/server/panel/vhost/rewrite/wutao.tech.conf;
    #REWRITE-END

    #禁止访问的文件或目录
    location ~ ^/(\.user.ini|\.htaccess|\.git|\.env|\.svn|\.project|LICENSE|README.md)
    {
        return 404;
    }

    #一键申请SSL证书验证目录相关设置
    location ~ \.well-known{
        allow all;
    }

    #禁止在证书验证目录放入敏感文件
    if ( $uri ~ "^/\.well-known/.*\.(php|jsp|py|js|css|lua|ts|go|zip|tar\.gz|rar|7z|sql|bak)$" ) {
        return 403;
    }

    # ========== API WebSocket 代理（在线人数功能） ==========
    location /api/ws/ {
        proxy_pass http://127.0.0.1:5922/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 3600s;
        proxy_buffering off;
    }

    # ========== API 反向代理 ==========
    location /api/ {
        proxy_pass http://127.0.0.1:5922/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ========== Vue Router history 模式支持 ==========
    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
    {
        expires      30d;
        error_log /dev/null;
        access_log /dev/null;
    }

    location ~ .*\.(js|css)?$
    {
        expires      12h;
        error_log /dev/null;
        access_log /dev/null;
    }
    access_log  /www/wwwlogs/wutao.tech.log;
    error_log  /www/wwwlogs/wutao.tech.error.log;
}
```

### 5.3 Nginx 配置 — 其他 3 个站点

`www.wutao.tech`、`admin.wutao.tech`、`cv.wutao.tech` 配置类似，区别：

- **不需要** `/api/ws/` WebSocket 段
- `server_name` 和 `root` 换成对应域名和目录
- 其余完全相同

通用模板（以 `admin.wutao.tech` 为例）：

```nginx
server
{
    listen 80;
    server_name admin.wutao.tech;
    index index.html;
    root /www/wwwroot/admin.wutao.tech;
    #CERT-APPLY-CHECK--START
    include /www/server/panel/vhost/nginx/well-known/admin.wutao.tech.conf;
    #CERT-APPLY-CHECK--END
    include /www/server/panel/vhost/nginx/extension/admin.wutao.tech/*.conf;
    
    #SSL-START
    #error_page 404/404.html;
    #SSL-END

    #ERROR-PAGE-START
    error_page 404 /404.html;
    #ERROR-PAGE-END

    #PHP-INFO-START
    include enable-php-00.conf;
    #PHP-INFO-END

    #REWRITE-START
    include /www/server/panel/vhost/rewrite/admin.wutao.tech.conf;
    #REWRITE-END

    location ~ ^/(\.user.ini|\.htaccess|\.git|\.env|\.svn|\.project|LICENSE|README.md)
    {
        return 404;
    }

    location ~ \.well-known{
        allow all;
    }

    if ( $uri ~ "^/\.well-known/.*\.(php|jsp|py|js|css|lua|ts|go|zip|tar\.gz|rar|7z|sql|bak)$" ) {
        return 403;
    }

    # ========== API 反向代理 ==========
    location /api/ {
        proxy_pass http://127.0.0.1:5922/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ========== Vue Router history 模式支持 ==========
    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
    {
        expires      30d;
        error_log /dev/null;
        access_log /dev/null;
    }

    location ~ .*\.(js|css)?$
    {
        expires      12h;
        error_log /dev/null;
        access_log /dev/null;
    }
    access_log  /www/wwwlogs/admin.wutao.tech.log;
    error_log  /www/wwwlogs/admin.wutao.tech.error.log;
}
```

### 5.4 Nginx 配置说明

| 配置段 | 作用 |
|---|---|
| `/api/ws/` | WebSocket 代理，博客在线人数功能需要（仅博客站） |
| `/api/` | 后端 API 反向代理，把 `/api/xxx` 请求转发到 5922 端口 |
| `/` | Vue Router history 模式，刷新页面不会 404 |

保存后宝塔会自动重载 Nginx。

---

## 6. 启动后端服务

### 6.1 启动命令（限制内存版）

```bash
cd /www/wwwroot/wutao
nohup java -Xms128m -Xmx384m -XX:MaxMetaspaceSize=192m -XX:MaxDirectMemorySize=128m -XX:+UseG1GC -jar WuTao-server-1.0-SNAPSHOT.jar --spring.profiles.active=prod > wutao.log 2>&1 &
```

### 6.2 JVM 参数说明

| 参数 | 值 | 说明 |
|---|---|---|
| `-Xms128m` | 初始堆内存 128MB | 启动时分配 |
| `-Xmx384m` | 最大堆内存 384MB | 限制上限 |
| `-XX:MaxMetaspaceSize=192m` | 元空间上限 192MB | 防止无限增长 |
| `-XX:MaxDirectMemorySize=128m` | 直接内存上限 128MB | NIO 缓冲区 |
| `-XX:+UseG1GC` | 使用 G1 垃圾回收器 | 适合小内存场景 |

### 6.3 验证启动

```bash
# 等待 35 秒（Spring Boot 启动需要时间）
sleep 35

# 确认进程在运行
ps -ef | grep WuTao

# 确认端口在监听
ss -tlnp | grep 5922

# 测试 API
curl http://127.0.0.1:5922
# 正常返回：{"code":0,"msg":"请求地址不存在","data":null}
```

### 6.4 查看日志

```bash
tail -50 /www/wwwroot/wutao/wutao.log
```

### 6.5 停止后端

```bash
# 查找进程 PID
ps -ef | grep WuTao

# 杀掉进程
kill -9 <PID>

# 或一次性杀掉所有 Java 进程
killall java
```

### 6.6 建议使用进程守护

宝塔 **软件商店** → 搜索安装 **进程守护管理器**（Supervisor），添加守护进程：

| 字段 | 值 |
|---|---|
| 名称 | `WuTao` |
| 启动命令 | `java -Xms128m -Xmx384m -XX:MaxMetaspaceSize=192m -XX:MaxDirectMemorySize=128m -XX:+UseG1GC -jar /www/wwwroot/wutao/WuTao-server-1.0-SNAPSHOT.jar --spring.profiles.active=prod` |
| 运行目录 | `/www/wwwroot/wutao` |

---

## 7. DNS 解析配置

### 7.1 添加解析记录

到阿里云域名控制台 → `wutao.tech` → **解析**，添加 4 条 A 记录：

| 记录类型 | 主机记录 | 解析请求来源 | 记录值 |
|---|---|---|---|
| A | `@` | 默认 | 服务器公网 IP |
| A | `www` | 默认 | 服务器公网 IP |
| A | `admin` | 默认 | 服务器公网 IP |
| A | `cv` | 默认 | 服务器公网 IP |

### 7.2 注意事项

- **解析请求来源** 必须选 **默认**，否则部分地区访问不了
- DNS 生效通常需要几分钟，最长 24 小时
- 可用 `nslookup wutao.tech` 验证是否生效

---

## 8. 2G 内存服务器优化

> 2核2G 服务器运行 MySQL + Redis + Spring Boot + Nginx，内存紧张，必须优化。

### 8.1 设置 Swap 交换空间

```bash
# 创建 2G swap 文件
dd if=/dev/zero of=/swapfile bs=1M count=2048

# 设置权限
chmod 600 /swapfile

# 格式化为 swap
mkswap /swapfile

# 启用 swap
swapon /swapfile

# 设置开机自动挂载
echo '/swapfile none swap sw 0 0' >> /etc/fstab

# 验证
free -h
```

### 8.2 优化 MySQL 配置

宝塔 → MySQL → **设置** → **配置修改**，在 `[mysqld]` 下调整：

```ini
[mysqld]
# 关键优化项
max_connections = 50              # 原 500 → 50
performance_schema = OFF          # 关闭，省约 100MB
skip-name-resolve                  # 取消注释，跳过 DNS 反查
key_buffer_size = 16M              # 原 32M → 16M
myisam_sort_buffer_size = 4M       # 原 8M → 4M
thread_cache_size = 8              # 原 16 → 8
tmp_table_size = 16M               # 原 32M → 16M
sort_buffer_size = 256K            # 原 768K → 256K
read_buffer_size = 256K            # 原 768K → 256K
innodb_log_file_size = 32M         # 原 64M → 32M
innodb_log_buffer_size = 8M        # 原 16M → 8M
innodb_buffer_pool_size = 128M     # 保持 128M
```

保存后重启 MySQL。

### 8.3 优化 Redis 配置

宝塔 → Redis → **设置** → **配置修改**：

```ini
maxmemory 128mb
maxmemory-policy allkeys-lru
```

保存后重启 Redis。

### 8.4 卸载不需要的软件

```bash
# 卸载 Tomcat 7（Spring Boot 不需要）
/www/server/tomcat7/bin/shutdown.sh
# 然后在宝塔软件商店中卸载 Tomcat 7
```

### 8.5 内存分配预估

| 组件 | 预估内存 |
|---|---|
| 系统 + 宝塔面板 | ~400MB |
| MySQL | ~300MB |
| Redis | ~50MB |
| Nginx | ~20MB |
| Spring Boot (JVM) | ~500MB |
| 剩余可用 | ~330MB + 2G Swap |

### 8.6 内存监控命令

```bash
# 查看内存使用
free -h

# 查看各进程内存占用
ps aux --sort=-%mem | head -10

# 查看 Swap 使用情况
swapon --show
```

---

## 9. SSL 证书配置（可选）

### 9.1 申请证书

每个站点 → **SSL** → **Let's Encrypt** → 申请并开启强制 HTTPS。

### 9.2 更新域名配置

SSL 申请成功后，修改 `application.yml` 中的域名：

```yaml
wutao:
  website:
    home: https://www.wutao.tech
    admin: https://admin.wutao.tech
    cv: https://cv.wutao.tech
    blog: https://wutao.tech
```

重新打包上传 JAR，重启后端。

---

## 10. 常见问题排查

### 10.1 后端启动失败

```bash
# 查看错误日志
tail -50 /www/wwwroot/wutao/wutao.log

# 常见原因：
# 1. MySQL 没启动 → systemctl status mysql
# 2. Redis 没启动 → systemctl status redis
# 3. 数据库密码不对 → 检查 application-prod.yml
# 4. JDK 版本不对 → java -version（需要 21+）
# 5. 配置文件不在同目录 → ls /www/wwwroot/wutao/
```

### 10.2 前端能访问但加载不出数据

```bash
# 1. 确认后端进程在运行
ps -ef | grep WuTao

# 2. 确认端口在监听
ss -tlnp | grep 5922

# 3. 直接测试后端 API
curl http://127.0.0.1:5922

# 4. 通过 Nginx 测试 API
curl http://127.0.0.1/api/ -H "Host: wutao.tech"

# 5. 浏览器 F12 → Network 查看 /api/ 请求状态码
```

### 10.3 宝塔面板访问不了

可能原因：内存不足导致面板崩溃。

```bash
# 杀掉 Java 进程释放内存
killall java

# 重启宝塔面板
bt restart
```

如果 SSH 也连不上了，到阿里云控制台重启实例。

### 10.4 页面刷新 404

Nginx 配置缺少 `location /` 的 `try_files` 规则。确认配置中有：

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

### 10.5 WebSocket 连接失败

博客在线人数不工作，检查 Nginx 配置中是否有 `/api/ws/` 段，且 `proxy_pass` 正确指向 `http://127.0.0.1:5922/ws/`。

### 10.6 DNS 未生效

```bash
# 验证 DNS 解析
nslookup wutao.tech
nslookup admin.wutao.tech

# 如果解析不到，检查阿里云 DNS 记录是否添加正确
# 解析请求来源必须选"默认"
```

### 10.7 多个 Java 进程冲突

```bash
# 查看所有 Java 进程
ps -ef | grep java

# 杀掉所有 Java 进程
killall java

# 确认已杀掉
ps -ef | grep java

# 重新启动一个
cd /www/wwwroot/wutao
nohup java -Xms128m -Xmx384m -XX:MaxMetaspaceSize=192m -XX:MaxDirectMemorySize=128m -XX:+UseG1GC -jar WuTao-server-1.0-SNAPSHOT.jar --spring.profiles.active=prod > wutao.log 2>&1 &
```

---

## 部署检查清单

| 步骤 | 状态 |
|---|---|
| 宝塔安装 Nginx / MySQL 8 / Redis 7 / JDK 21 | ☐ |
| 卸载 Tomcat 7（不需要） | ☐ |
| 创建数据库 `personal`，导入 wutao.sql | ☐ |
| 配置 application-prod.yml（IP 改 127.0.0.1） | ☐ |
| application.yml 切换 active: prod | ☐ |
| application.yml 配置域名 | ☐ |
| 上传 JAR 包 + 配置文件到服务器 | ☐ |
| 构建 4 个前端 dist 并上传 | ☐ |
| 创建 4 个站点 | ☐ |
| 配置 4 个站点 Nginx（API 代理 + history 模式） | ☐ |
| 设置 Swap 交换空间 | ☐ |
| 优化 MySQL / Redis 内存配置 | ☐ |
| 启动后端（限制 JVM 内存） | ☐ |
| 验证后端 curl http://127.0.0.1:5922 | ☐ |
| 添加 4 条 DNS 解析记录 | ☐ |
| 浏览器测试访问 | ☐ |
| 申请 SSL 证书 | ☐ |
| 域名改为 https:// 并重新部署 | ☐ |

---

## 服务器信息

| 项目 | 值 |
|---|---|
| 服务器 IP | 8.153.155.254 |
| 配置 | 2核 2G |
| 域名 | wutao.tech |
| 后端端口 | 5922 |
| 数据库 | personal |
| 数据库用户 | personal |
| Redis 端口 | 6379 |
| 后端部署路径 | /www/wwwroot/wutao/ |
| 博客站路径 | /www/wwwroot/wutao.tech/ |
| 管理端路径 | /www/wwwroot/admin.wutao.tech/ |
| 主页路径 | /www/wwwroot/www.wutao.tech/ |
| 简历路径 | /www/wwwroot/cv.wutao.tech/ |
